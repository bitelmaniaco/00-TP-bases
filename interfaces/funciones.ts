(() => {

    interface meterDosNumeros {
        (a: number, b: number): number;
    }

    let metiendoDos: meterDosNumeros;

    metiendoDos = (a: number, b: number) => {
        return 10;
    }


})()